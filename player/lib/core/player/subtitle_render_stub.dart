/// Web half of `subtitle_render.dart`. media_kit's web backend drives an
/// `HTMLVideoElement`; there is no mpv to hand bitmaps to.
library;

import 'package:media_kit/media_kit.dart';

Future<void> watchSubtitleRendering(Player player) async {}
