query StreamingCandidates($contentType: String!, $id: ID!) {
  streamingCandidates(contentType: $contentType, id: $id) {
    fileId
    candidates {
      strategy
      mime
      container
      videoCodec
      audioCodec
    }
    metadata {
      duration
      width
      height
    }
  }
}
