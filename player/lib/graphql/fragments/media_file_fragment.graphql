fragment MediaFileFragment on MediaFile {
  id
  resolution
  codec
  audioCodec
  hdrFormat
  size
  bitrate
  directPlaySupported
  streamUrl
  directPlayUrl
  subtitles {
    trackId
    language
    title
    format
    embedded
    deliverable
    url(format: VTT)
  }
}
