mutation VerifyTotp($challengeToken: String!, $code: String!) {
  verifyTotp(input: { challengeToken: $challengeToken, code: $code }) {
    token
    user {
      id
      username
      email
      displayName
    }
    expiresIn
    totpRequired
  }
}
