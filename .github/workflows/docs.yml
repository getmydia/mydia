name: Documentation

on:
  push:
    branches:
      - master
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - '.github/workflows/docs.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., 1.0.0 or dev)'
        required: false
        default: 'dev'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --frozen --project docs

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Fetch gh-pages branch
        run: |
          git fetch origin gh-pages:gh-pages || echo "gh-pages branch does not exist yet"

      - name: Deploy latest (push to master)
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        run: |
          uv run --project docs mike deploy --push --update-aliases dev
          uv run --project docs mike set-default --push dev

      - name: Cleanup stale latest version
        if: github.event_name == 'release'
        run: |
          # Delete "latest" if it exists as a version (from old workflow bug)
          # This allows us to use "latest" as an alias instead
          uv run --project docs mike delete --push latest || true

      - name: Deploy release version
        if: github.event_name == 'release'
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          # Remove 'v' prefix if present
          VERSION="${VERSION#v}"
          uv run --project docs mike deploy --push --update-aliases "$VERSION" latest
          uv run --project docs mike set-default --push latest

      - name: Deploy manual version
        if: github.event_name == 'workflow_dispatch'
        run: |
          VERSION="${{ github.event.inputs.version }}"
          if [ "$VERSION" = "dev" ]; then
            uv run --project docs mike deploy --push --update-aliases dev
            uv run --project docs mike set-default --push dev
          else
            # Delete stale "latest" version if it exists (from old workflow bug)
            uv run --project docs mike delete --push latest || true
            uv run --project docs mike deploy --push --update-aliases "$VERSION" latest
            uv run --project docs mike set-default --push latest
          fi
