apiVersion: v1
kind: ConfigMap
metadata:
  name: metadata-relay-config
  namespace: metadata-relay
  labels:
    app.kubernetes.io/name: metadata-relay
    app.kubernetes.io/component: relay-service
data:
  # Application configuration
  PORT: "4000"
  PHX_HOST: "relay.mydia.dev"
  MIX_ENV: "prod"

  # Database configuration
  SQLITE_DB_PATH: "/data/metadata_relay.db"

  # Redis cache for distributed/persistent caching
  REDIS_URL: "redis://redis:6379"

  # Feedback email notifications (delivered via Resend SMTP).
  # The matching SMTP_PASSWORD (Resend API key) lives in the secret.
  FEEDBACK_EMAIL_TO: "feedback@mydia.dev"
  FEEDBACK_EMAIL_FROM: "feedback@mydia.dev"
  FEEDBACK_DASHBOARD_URL: "https://relay.mydia.dev"
  SMTP_HOST: "smtp.resend.com"
  SMTP_PORT: "587"
  SMTP_USERNAME: "resend"

  # Iroh relays served at /client-config, comma-separated. Unset serves the
  # built-in list in MetadataRelay.ClientConfig. Read at boot; see the
  # README's "Changing the relay list".
  # CLIENT_CONFIG_RELAYS: "https://cae1-1.relay.mydia.dev"
