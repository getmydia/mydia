apiVersion: apps/v1
kind: Deployment
metadata:
  name: iroh-relay
  namespace: iroh-relay
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: iroh-relay
      app.kubernetes.io/part-of: mydia
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iroh-relay
        app.kubernetes.io/part-of: mydia
      annotations:
        # Picked up by the monitoring stack's `kubernetes-pods` scrape job.
        # relayserver_* counters are process-local and reset on restart, so
        # without scraping there is no history of relay usage at all.
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: iroh-relay
          image: n0computer/iroh-relay:v1.0.3@sha256:e9a7ba14287dd9693aca61c339b0d154456d6609f3818a0ef358b07b36aab9cc
          args: ["--config-path", "/config/config.toml"]
          ports:
            - name: http
              containerPort: 80
            - name: https
              containerPort: 443
            - name: quic
              containerPort: 7842
              protocol: UDP
              hostPort: 7842
            - name: stun
              containerPort: 3478
              protocol: UDP
              hostPort: 3478
            - name: metrics
              containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: certs
              mountPath: /certs
              readOnly: true
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /generate_204
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /generate_204
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: iroh-relay-config
        - name: certs
          secret:
            secretName: iroh-relay-tls-secret
        - name: data
          persistentVolumeClaim:
            claimName: iroh-relay-data
