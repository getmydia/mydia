[package]
name = "mydia-rs-p2p"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# native/mydia_p2p_core is consumed as a path dep when U29 ports the p2p seam.
# Intentionally not wired in U1 to keep the workspace independently buildable.
