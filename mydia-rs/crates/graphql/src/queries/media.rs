//! Media field resolvers — port of
//! `lib/mydia_web/schema/resolvers/media_resolver.ex`. Lands in
//! U10.c via `#[ComplexObject]` impls attached to the Movie / TvShow
//! / Episode / Season types. The empty module here documents the
//! seam.
