[package]
name = "mydia_torrent_core"
version = "0.1.0"
edition = "2021"

[dependencies]
librqbit = { version = "8", default-features = false, features = ["rust-tls"] }
tokio = { version = "1.49", features = ["full"] }
anyhow = "1.0"
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
futures = "0.3"
uuid = { version = "1.20", features = ["v4"] }
hex = "0.4"


[profile.release]
lto = "thin"
codegen-units = 1
opt-level = "z"
strip = true
panic = "abort"
